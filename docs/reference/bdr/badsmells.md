# 坏味道规约文档（BDR Bad Smells）

**版本**：0.7.0  
**状态**：草案 — 待维护者确认  
**依据**：[constitution.md](./constitution.md) **v1.5**、[specification.md](./specification.md) **v0.2.7**  
**修订日期**：2026-04-15  


| 项目       | 说明                                                                                                                 |
| -------- | ------------------------------------------------------------------------------------------------------------------ |
| **文档定位** | BDR **坏味道（badsmells）环节** 的 **正式规约载体**（具体条目）；命名与路径以 [constitution.md §2](./constitution.md#2-bdr-权威工件与坏味道规约文档) 为权威。 |
| **统计范围** | 以 `src/eywa` 为主，兼及 `examples/` 与 SDD 对照（随迭代更新）                                                                     |
| **关联任务** | 重构任务见 [tasks.md](./tasks.md)；须与本文件条目 **可追溯对应**。                                                                    |


---

## 1. 说明

- 条目 **结构、ID、SDD 联动** 等格式要求见 [specification.md](./specification.md)。  
- **BDR 执行与任务拆分** 以 **本文件（[badsmells.md](./badsmells.md)）** 为 **唯一** 权威源。增删改条目时须 **更新本文件版本与修订说明**，并走 [analysis.md](./analysis.md) 同步 [tasks.md](./tasks.md)。  
- **识别坏味道** 环节每次 **升版** 或 **在 §3 修订历史中新增一行** 时，须按 [specification.md §7](./specification.md#7-修订历史表与提交版本门禁) 填写 **提交版本**（`git rev-parse HEAD`）。
- **已消除** 条目的正文 **可保留** 作审计基线；**当前是否仍构成坏味道** 以 **§2.0 索引** 与代码为准。

---

## 2. 坏味道条目

### 2.0 索引与消除状态（相对 `tasks.md` B-T 进度）


| BS-ID              | 状态       | 说明                                                                                              |
| ------------------ | -------- | ----------------------------------------------------------------------------------------------- |
| **BS-CLARITY-001** | **部分残余** | **B-T05** 已提取 `SpiritSeerCatalog`；`Spirit` 仍偏大（**约 613** 行，随迭代见正文）。                             |
| **BS-CLARITY-002** | 已消除      | **B-T04**（`carry_out` 分支拆分）。                                                                    |
| **BS-REUSE-001**   | 已消除      | **B-T01**（`_bootstrap_from_filled_registry`）。                                                   |
| **BS-REUSE-002**   | 已消除      | **B-T03**（`load_yaml_root_mapping` 统一入口）；**§2 消除标准** 已补全（原表格截断已修复）。                                                       |
| **BS-ROBUST-001**  | 已消除      | **B-T06**（工具日志 + 流式 SSE 策略）。                                                                    |
| **BS-SEC-001**     | 已消除      | **B-T07**（HTTP 失败脱敏日志 + `base_url` 注册校验）。                                                       |
| **BS-SIMPLE-001**  | 已消除      | **B-T02** / **B-T05**：`spirit.py` **顶导入 `yaml`**；**残余** 见 **BS-SIMPLE-002**。                                                         |
| **BS-ROBUST-002**  | 已消除      | **B-T08**（`complete_with_network_retry` 非预期异常与重试用尽路径 **WARNING** + `**redact_secrets`**）。       |
| **BS-SEC-002**     | 已消除      | **B-T09**（`**OpenAICompatibleClient.__init__`** 调用 `**validate_llm_base_url**`，与 Registry 同策略）。 |
| **BS-CLARITY-003** | 已消除      | **B-T10**；`serial_multi_agent.py` **约 204** 行（**`yaml_mapping`** 分流后），见条目正文归档。                                                                                        |
| **BS-SIMPLE-002**  | **部分残余** | **B-T11** 已收敛 **YAML** 加载；**残余** 为 **`Spirit.link`** 路径 **非 yaml** 延迟导入（循环依赖规避），见条目正文。                                                                                        |
| **BS-CLARITY-004** | 已消除      | **B-T12**（`Eywa.kindle` 分支收敛；与 **M5** 前基线对齐）。                                                                                        |
| **BS-CLARITY-005** | 已消除      | **B-T13**（`perception` 包内拆分或体量已可接受）。                                                                                        |
| **BS-CLARITY-006** | 已消除      | **B-T14**（`Planner` schema/抽取拆分）。                                                                                        |
| **BS-CLARITY-007** | 已消除      | **B-T15**（`WorkflowEngine` 分派可扩展化）。                                                                                        |
| **BS-CLARITY-008** | 已消除      | **B-T16**（foundation 依赖与内置技能双轨书面闭环）。                                                                                        |
| **BS-CONSIST-001** | **部分残余** | **B-T17**：`type: ignore` **已收窄** 至 **1** 处（`listen_ontology`）；包级 **`__all__`** 宽幅面仍可继续收窄，见条目正文。                                                                                        |
| **BS-ROBUST-003**  | 已消除      | **B-T18**（PDF / HTTP 辅助路径可观测）。                                                                                        |
| **BS-CLARITY-009** | 已消除      | **B-T19**：`Eywa` 母体静态方法收敛为 `MotherOrchestration` 权威实现 + `Eywa` 薄别名委托。                                                                                        |
| **BS-REUSE-003**   | 已消除      | **B-T20**：`bootstrap` 四路径收敛到单一 `.env.yaml` 合并与六元组装配入口。                                                                                        |
| **BS-CLARITY-010** | 已消除      | **B-T21**：`_profile_from_yaml_item` 拆分为新建与覆盖分支函数。                                                                                        |


---

### BS-CLARITY-001 — `Spirit` 类职责与体量偏大


| 字段           | 内容                                                                                                                                                                                                                              |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **位置**       | `src/eywa/kernel/core/spirit.py`（`Spirit` 全类）                                                                                                                                                                                   |
| **描述**       | 单类仍集中：`carry_out` 多语义分派、记忆与知识库、灵魂树与会话、`enter_seer_roles` / 工具链等与 Runtime/Crew 的衔接。洞悉角色与 YAML **agents/顺序** 已委托 `**SpiritSeerCatalog**`（**B-T05**），但 **Large Class** 与 **多轴心** 气味仍存（**`spirit.py` 全文件约 613 行**，随功能迭代上升，依赖面广）。                        |
| **对齐原则**     | 宪法 §3.1 **清晰**；§3.4 **复用**（**单一职责**、**高内聚**）                                                                                                                                                                                    |
| **消除标准（验收）** | 在 **不改变** `Spirit` **对外公开方法签名与可观察行为**（与现有 `tests/test_m16_spirit_api.py`、`tests/test_m17_kindle_batteries.py` 等一致）的前提下，将 **至少一类** 独立关切（例如：洞悉角色与 YAML 缓存状态、或 `carry_out` 分派）提取为 **独立模块内** 的协作对象或命名空间；`Spirit` 主体以委托为主；**全量测试绿**。 |
| **风险与约束**    | 若提取导致 **公开 API** 或 **文档化行为** 变化 → **[SDD]** 须先修订并验收。                                                                                                                                                                            |


---

### BS-CLARITY-002 — `Spirit.carry_out` 长方法与分支复杂度高


| 字段           | 内容                                                                                                                                     |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------- |
| **位置**       | `src/eywa/kernel/core/spirit.py` — `carry_out` 及其直接支撑私有方法                                                                              |
| **描述**       | `carry_out` 内集中 **串行 / Crew 单槽 / 单轮** 三路逻辑与大量参数合法性校验，呈现 **Long Method**、**复杂条件链**（类 Switch）气味；与 BS-CLARITY-001 相关但可 **单独** 以「方法级拆分」消除。 |
| **对齐原则**     | 宪法 §3.1 **清晰**；§3.8 **简单**（控制流可读）                                                                                                      |
| **消除标准（验收）** | 将三路执行与校验拆为 **具名私有方法或小型策略函数**（仍属 `spirit` 包内即可）；单函数认知负担明显下降（建议：各分支主体 ≤ ~40 行量级或圈复杂度可测下降）；**行为与现有测试一致**、**全量测试绿**。                       |
| **风险与约束**    | 无行为变更预期；若误改 `carry_out` 语义 → 走 SDD / 测例增补。                                                                                             |


---

### BS-REUSE-001 — `bootstrap_from_env` 与 `bootstrap_from_openai_compat_serial_env` 结构重复


| 字段           | 内容                                                                                                                                                                                                                  |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **位置**       | `src/eywa/kernel/core/bootstrap.py`                                                                                                                                                                                 |
| **描述**       | 两个函数除 **profile 注册入口**（`register_llm_profiles_from_env` vs `register_openai_compat_serial_profiles`）外，其余 **「registry → ToolRegistry → OpenAICompatibleClient → build_runtime_and_crew → 五元组返回」** 路径几乎相同，违背 **DRY**。 |
| **对齐原则**     | 宪法 §3.4 **复用**                                                                                                                                                                                                      |
| **消除标准（验收）** | 提取共用私有或模块级函数，使两条公开路径 **仅差「如何填充 registry 与 default_pid」**；返回类型与字段顺序不变；`**test_m17_kindle_batteries` 及相关 kindle 测例全绿**。                                                                                               |
| **风险与约束**    | 不改变环境变量约定与默认 profile 解析语义；若语义变化 **[SDD]**。                                                                                                                                                                          |


---

### BS-REUSE-002 — 流水线 YAML / dict 根类型校验分散


| 字段           | 内容                                                                                                                                |
| ------------ | --------------------------------------------------------------------------------------------------------------------------------- |
| **位置**       | 历史：`src/eywa/kernel/core/spirit.py`；**当前实现**：`src/eywa/kernel/core/spirit_seer_catalog.py` — **`load_yaml_root_mapping`**（**B-T03**）。   |
| **描述**       | （**已消除 — 归档**）「根必须为 mapping」类校验与加载曾在 **路径** 与 **dict 直通** 两处重复，错误类型/文案易不一致。**B-T03** 后统一经 **`load_yaml_root_mapping`**，由调用方选择 **`ValueError`** / **`SerialMultiAgentSpecError`** 等契约。 |
| **对齐原则**     | 宪法 §3.4 **复用**；§3.1 **清晰**（单一入口）                                                                                                  |
| **消除标准（验收）** | **已满足（B-T03）**：**单一入口** 加载 YAML 路径或已有 **`Mapping`**；`Spirit` 串行 spec 与 **`enter_seer_roles`** 均经该入口；**异常类型与消息** 与 **`tests/`** 及文档化可观察行为一致。**回归**：不得在无任务批准下恢复双轨根校验逻辑。若未来变更入口或异常契约 → 须 **[SDD]** 或版本说明。                                                                          |
| **风险与约束**    | 若统一后 **异常类型** 从调用方可见角度发生变化 → 视为行为变更，须 **[SDD]** 或明确版本说明。                                                                          |


---

### BS-ROBUST-001 — 关键路径错误缺少分级日志与可观测性


| 字段           | 内容                                                                                                                                                                                                                                   |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **位置**       | `src/eywa/kernel/action/tooling.py` — `ToolInvoker.invoke`（Python 工具分支）；`src/eywa/foundation/llm/client.py` — `OpenAICompatibleClient._complete_streaming`（SSE 行解析）                                                                  |
| **描述**       | **工具层**：捕获 `Exception` 后仅将 `str(e)` 写入 `ToolResult.error` 返回调用方，**无** 结构化日志（工具 id、异常类型、经脱敏的摘要），运维与排障依赖上层偶然打印，违背 **分级处理 + 日志记录** 的健壮性要求。**流式 LLM**：对 `json.loads` 的 `JSONDecodeError` **静默 `continue`**，协议异常或中间人损坏可能被吞掉，难以与「可恢复噪声」区分。 |
| **对齐原则**     | 宪法 §3.6 **健壮**（分级异常、日志、避免无声失败）                                                                                                                                                                                                       |
| **消除标准（验收）** | **工具**：失败路径至少 **记录** 一条带 `tool_id`、异常类型的日志（级别与是否经 `redact_secrets` 在任务中固定）；行为与 `ToolResult` 契约不变、相关测例绿。**流式客户端**：对解析失败采用 **可配置策略**（例如：计数 + DEBUG/WARNING 日志，或累计超限后失败），并有测例覆盖「损坏 SSE 行」场景；不改变正常流式补全的可观察输出。                            |
| **风险与约束**    | 若新增日志默认打到控制台且含敏感信息 → 须与 **BS-SEC-001** 协同；对外 API 不变则 **非 SDD**。                                                                                                                                                                      |


---

### BS-SEC-001 — 出站 URL 与日志/异常中的机密治理未闭环


| 字段           | 内容                                                                                                                                                                                                                                                                                    |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **位置**       | `src/eywa/foundation/llm/env_bootstrap.py`、`openai_compat_serial_env.py`（及 JSON 注册路径）；`src/eywa/kernel/intelligence/sanitize.py` — `redact_secrets`；`src/eywa/foundation/llm/client.py`；智能层异常与日志调用点（若有）                                                                               |
| **描述**       | `**redact_secrets` 已实现** 但 **生产路径未接入**（当前主要仅测例引用），与宪法 **§3.7**「敏感数据脱敏、密钥不入明文日志」不一致。`**base_url`** 可由 `EYWA_LLM_PROFILES_JSON` / 环境变量注入 **任意 HTTP(S) 目标**，缺少 **scheme 约束、内网 SSRF 护栏或显式「可信配置源」文档/校验**，与 **输入与输出 / 最小暴露** 原则存在差距（尤其在多租户或共享运行环境设想下）。                                      |
| **对齐原则**     | 宪法 §3.7 **安全**（机密与隐私、输入与输出、最小权限）                                                                                                                                                                                                                                                      |
| **消除标准（验收）** | **脱敏**：在 **至少一条** 会记录 LLM/HTTP 失败或响应摘要的路径上调用 `redact_secrets`（或等价集中封装），并有测例断言 **Bearer / api_key 模式** 被弱化。**URL**：在注册 `LlmProfile` 时校验 `base_url` 为 **http/https**（或项目明确允许的 scheme），可选 **禁止 obvious 内网/元数据地址**（策略在任务中写清）；**或** 在 SDD/安全说明中 **显式记载**「配置侧可信」并配套集成测；**现有合法配置** 行为不变、测例绿。 |
| **风险与约束**    | 若校验过严导致合法 `localhost`/内网开发配置失效 → 用 **允许列表** 或 **环境开关** 区分；若改变 profile 注册抛错条件 → 评估 **[SDD]**。                                                                                                                                                                                          |


---

### BS-SIMPLE-001 — `Spirit` 内多处延迟 `import yaml`


| 字段           | 内容                                                                                                              |
| ------------ | --------------------------------------------------------------------------------------------------------------- |
| **位置**       | `src/eywa/kernel/core/spirit.py`（**B-T02** 后 **顶导入 `yaml`**；**不再** 在 `_carry_out_load_serial_spec` / `enter_seer_roles` 内重复 `import yaml`）。                           |
| **描述**       | （**已消除 — 归档**）**B-T02** 已将 `yaml` 提升为 **`spirit.py` 模块顶导入**。**残余** 同类气味见 **BS-SIMPLE-002**（其它文件中的延迟导入 / 不一致 YAML 入口）。                          |
| **对齐原则**     | 宪法 §3.8 **简单**；§3.3 **可读**                                                                                      |
| **消除标准（验收）** | **已满足（B-T02）**：`spirit.py` 无函数体内重复 `import yaml`（或 docstring 记载保留延迟加载原因）。**回归** 见 **BS-SIMPLE-002**。 |
| **风险与约束**    | 若顶导入引入 **导入环** → 保留延迟加载并 **文档记载原因** 即可验收本条「显式降级」。                                                               |


---

### BS-ROBUST-002 — `complete_with_network_retry` 非 HTTP 异常路径缺少可观测脱敏


| 字段           | 内容                                                                                                                                                                                                                                                                                                          |
| ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **位置**       | `src/eywa/kernel/intelligence/llm_retry.py` — `complete_with_network_retry`                                                                                                                                                                                                                                 |
| **描述**       | 对 `**httpx.TimeoutException`**、**5xx** 的 `**HTTPStatusError`** 有退避重试；对其它异常走 `**except Exception**`，直接 `**raise IntelligenceError(f"LLM call failed: {e}")**`。**无** 结构化日志（尝试次数、异常类型、脱敏摘要），`**str(e)` 未经** `redact_secrets`，与 **B-T06** 后工具层、`**OpenAICompatibleClient`** 的 HTTP 失败日志策略 **不一致**，排障时依赖调用方偶然打印。 |
| **对齐原则**     | 宪法 §3.6 **健壮**；§3.2 **一致**（错误处理与可观测性对齐）                                                                                                                                                                                                                                                                     |
| **消除标准（验收）** | 在 **不改变** 重试语义与 `**IntelligenceError`** 对外契约（除非测例与 SDD 明确升级）的前提下：至少对 **最后一次失败** 或 **非 httpx 预期类型** 记录 **WARNING**（或项目选定的级别），消息经 `**redact_secrets`**；**全量测试绿**；若有 `**caplog`** 测例则断言 **无明文密钥模式**。                                                                                                           |
| **风险与约束**    | 若扩展异常类型分支 → 注意与 `**OpenAICompatibleClient`** 内已有日志 **避免重复刷屏**；对外 API 变更 → **[SDD]**。                                                                                                                                                                                                                        |


---

### BS-SEC-002 — `base_url` 校验未覆盖「不经 Registry 构造 Client」路径


| 字段           | 内容                                                                                                                                                                                                                                                                                                                                   |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **位置**       | `src/eywa/foundation/llm/client.py` — `OpenAICompatibleClient`；`src/eywa/foundation/llm/registry.py` — `validate_llm_base_url`（**B-T07**）                                                                                                                                                                                            |
| **描述**       | `**LlmProfileRegistry.register`** 已校验 `**base_url**`（scheme/主机/可选私网开关），但 `**OpenAICompatibleClient(profile, …)**` 可 **绕过** Registry **直接使用** `LlmProfile`。测试、工厂与高级注入场景合理，但与「凡出站 LLM 端点均经同一注册护栏」的 **心智模型** 不完全一致；恶意或错误配置仍可能以 **非 http(s)** 或不当主机到达 HTTP 层才失败。                                                                         |
| **对齐原则**     | 宪法 §3.7 **安全**；§3.2 **一致**（入口与策略一致）                                                                                                                                                                                                                                                                                                  |
| **消除标准（验收）** | **任选其一** 并文档化：**(a)** 在 `**OpenAICompatibleClient.__init__`** 中调用与 Registry **相同** 的 `**validate_llm_base_url`**（或共享校验函数），**现有合法测例** 行为不变；**(b)** 在 SDD/本仓库安全说明中 **显式记载**「仅经 `**register_*_from_env` / Registry** 的配置受护栏约束」，并限制生产入口；**(c)** 组合：(a) 对 `profile` 做轻量校验 + (b) 说明测试直连例外。配套测例覆盖 **绕过 Registry** 的非法 URL **应** 在选 (a) 时失败。 |
| **风险与约束**    | (a) 可能改变 **非法 profile** 的失败阶段（由 HTTP 变为构造期 `**ValueError`**）→ 评估 **[SDD]** 与测例。                                                                                                                                                                                                                                                      |


---

### BS-CLARITY-003 — `serial_multi_agent` 单模块多职责仍可分层（**已消除 — 归档**）


| 字段           | 内容                                                                                                                                                                                                                                           |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **位置**       | `src/eywa/kernel/intelligence/serial_multi_agent.py`；**`yaml_mapping.load_yaml_root_mapping`** 分流                                                                                                                                                                                         |
| **描述**       | （**已消除 — B-T10**）原 **Long Module** 风险已通过 **`yaml_mapping`** 与职责收敛 **显著下降**（**全文件约 204 行**，以仓库当前为准）。结构校验、槽位解析仍 **同模块** 并列，若未来再膨胀可 **重新打开** 子模块拆分。 |
| **对齐原则**     | 宪法 §3.1 **清晰**；§3.8 **简单**（模块边界）                                                                                                                                                                                                             |
| **消除标准（验收）** | **已满足（B-T10）**：体量与 **YAML 根加载** 与 **`load_yaml_root_mapping`** 对齐；**全量测试绿**。                                                                                                         |
| **风险与约束**    | 若公开符号路径变化 → **[SDD]** 或 re-export 过渡。                                                                                                                                                                                                        |


---

### BS-SIMPLE-002 — 延迟导入与 `yaml` 入口仍不一致（**部分残余**）


| 字段           | 内容                                                                                                                                                                                                                                           |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **位置**       | `src/eywa/kernel/core/spirit.py` — `Spirit.link` 路径（`**register_skill_from_path**`、**`build_crew_tools_from_registry**` 函数体内延迟导入**，规避 **open/skills ↔ crewai_integration** 与 **`Spirit`** 的循环依赖）。**YAML**：`load_serial_multi_agent_yaml` 已经 **`load_yaml_root_mapping`**（**B-T11**）。                                                                                                                                                                                         |
| **描述**       | **`yaml` 加载** 已 **单一入口**；**残余** 为 **非 YAML** 的 **延迟导入**（仍属 **一致** 与 **简单** 张力）。若未来 **依赖图** 允许，宜 **顶导入** 或 **显式** `TYPE_CHECKING` 分包。 |
| **对齐原则**     | 宪法 §3.8 **简单**；§3.2 **一致**（同类能力单一入口）；§3.3 **可读**                                                                                                                                                                                                             |
| **消除标准（验收）** | **任选**：消除 **`Spirit.link`** 内延迟导入（重构依赖方向或 **facade 模块**）；**或** 在 **`Spirit.link` docstring** 与 **`badsmells` 本条** 固定「为何延迟」**一行** 说明，视为 **显式降级** 验收。**行为与全量 `pytest` 绿**。                                                                                                         |
| **风险与约束**    | 顶导入若引入 **循环依赖** → 保留延迟加载并在 **docstring** 记载原因。                                                                                                                                                                                                        |


---

### BS-CLARITY-004 — `Eywa.kindle` 长参数列表与多分支电池化（**已消除 — 归档**）


| 字段           | 内容                                                                                                                                                                                                                                           |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **位置**       | `src/eywa/kernel/core/eywa.py` — `Eywa.kindle`；**`eywa_kindle.kindle_bootstrap`**                                                                                                                                                                                         |
| **描述**       | （**已消除 — B-T12**；**M5-02** 后 **电池化** 与 **`.env.yaml`** 经 **`KindleBootstrap` / `bootstrap`** 延伸，仍须与 **BS-CLARITY-009** 区分。）原 **Long Parameter List** 已通过 **`kindle_bootstrap`** 等 **委托** 缓解；若 **`kindle`** 再次膨胀，**重新评估** 本条。 |
| **对齐原则**     | 宪法 §3.1 **清晰**；§3.8 **简单**                                                                                                                                                                                                             |
| **消除标准（验收）** | **已满足（B-T12）**：主路径 **委托**、测例绿；后续以 **BS-CLARITY-009** 跟踪 **母体 API** 面。                                                                                                         |
| **风险与约束**    | 任何 **默认行为** 或 **参数互斥** 语义变化 → **[SDD]**。                                                                                                                                                                                                        |


---

### BS-CLARITY-005 — `PerceptionService` / `perception.py` 多职责同模块（**已消除 — 归档**）


| 字段           | 内容                                                                                                                                                                                                                                           |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **位置**       | `src/eywa/kernel/semantics/perception.py`                                                                                                                                                                                         |
| **描述**       | 同文件并列：**视图/dataclass**、指纹、知识合并与检索、**skill L1** 文本、可注入回调等，**单模块修改面** 宽，呈现 **Large Module** / **Feature Envy** 风险。 |
| **对齐原则**     | 宪法 §3.1 **清晰**；§3.4 **复用**（单一职责）                                                                                                                                                                                                             |
| **消除标准（验收）** | 将 **纯格式化/拼接**、**检索编排** 或 **视图构建** 之一拆至 **同包** 子模块（`perception_*` 或 `semantics` 下二级模块），**`PerceptionService.build_view` 对外契约** 不变；**全量测试绿**；若 **`SemanticView` 字段** 或 **JSON 快照** 可观察变化 → **[SDD]**。                                                                                                         |
| **风险与约束**    | 与 **FR-S-02/03**、**design §6.2～§6.3** 锁定的分隔符与顺序冲突时须先 **SDD**。                                                                                                                                                                                                        |


---

### BS-CLARITY-006 — `Planner` 模块内 Schema / JSON 抽取 / 纠错堆叠（**已消除 — 归档**）


| 字段           | 内容                                                                                                                                                                                                                                           |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **位置**       | `src/eywa/kernel/intelligence/planner.py`                                                                                                                                                                                         |
| **描述**       | **内嵌大型 `PLAN_JSON_SCHEMA`**、`extract_plan_json_object`、schema 失败 **纠错**、**tool_id** 语义 **再纠错** 等同文件堆叠，**认知与测试锚点** 集中，后续演进易 **牵一发而动全身**。 |
| **对齐原则**     | 宪法 §3.1 **清晰**；§3.8 **简单**                                                                                                                                                                                                             |
| **消除标准（验收）** | 将 **Schema 常量**、**JSON 提取**、**纠错消息构造** 拆为 **同包** 子模块或纯函数文件；**`Planner.plan` 行为与 `Plan` 契约** 不变；**全量测试绿**。若计划 JSON **对外格式** 变化 → **[SDD]**。                                                                                                         |
| **风险与约束**    | 与 **FR-I-01/02**、**Planner** 测例断言的 **messages 片段** 冲突时须同步测例或 SDD。                                                                                                                                                                                                        |


---

### BS-CLARITY-007 — `WorkflowEngine` 步骤类型分支的扩展面（**已消除 — 归档**）


| 字段           | 内容                                                                                                                                                                                                                                           |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **位置**       | `src/eywa/kernel/intelligence/workflow/engine.py`；协同 `workflow/spec.py`                                                                                                                                                                                         |
| **描述**       | 按 **`step.type`** 分支执行（**delay / if / loop / python_tool** 等），呈 **隐式 switch**；控制流类型继续增加时，易出现 **Shotgun Surgery**（引擎与解析双处修改）。 |
| **对齐原则**     | 宪法 §3.1 **清晰**；§3.5 **扩展**（开闭）                                                                                                                                                                                                             |
| **消除标准（验收）** | **任选**：**表驱动**分派（`dict[type, Handler]`）、**每类步骤策略类**、或 **独立模块** 承载 `_run_step` 子逻辑；**`execute` 产出 `ctx` 与 `_executed_step_ids` 等可观察契约** 不变；**`tests/kernel/test_workflow_session.py`** 等全绿。若 **工作流语义** 变化 → **[SDD]**。                                                                                                         |
| **风险与约束**    | 与 **M2-01 / design §7.3** 锁定语义冲突时须先 **SDD**。                                                                                                                                                                                                        |


---

### BS-CLARITY-008 — `foundation→kernel` 依赖与内置技能双轨目录（**已消除 — 归档**）


| 字段           | 内容                                                                                                                                                                                                                                           |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **位置**       | `src/eywa/foundation/crewai_integration/eywa_tool_bridge.py`（依赖 `kernel.action`）；`src/eywa/open/skills/builtin/` 与 `src/eywa/open/skills/builtin_packages/`                                                                                                                                                                                         |
| **描述**       | **基础层** 对 **内核层** 的依赖 **倒置** 典型气味（若未在 SDD 明确为 **允许的例外**）；内置技能 **两套目录/入口**（`builtin` 与 `builtin_packages`）增加 **导航成本** 与 **重复概念**。 |
| **对齐原则**     | 宪法 §3.1 **清晰**（边界）；§3.5 **扩展**（DIP）；§3.2 **一致**                                                                                                                                                                                                             |
| **消除标准（验收）** | **依赖方向**：**任选** — 将桥接 **下沉** 至 `kernel`/`open` 并令 `foundation` 仅依赖协议；**或** 在 **SDD / design** 中单节 **书面批准** 当前例外并 **冻结新增** 同类依赖。**双轨**：**合并目录**、**文档化单一权威入口**，或 **deprecation 别名** 一版；**全量测试绿**。                                                                                                         |
| **风险与约束**    | 移动模块 **公开导入路径** 变化 → **[SDD]** 或 **re-export 过渡**。                                                                                                                                                                                                        |


---

### BS-CONSIST-001 — 类型忽略与包级再导出宽幅面（**部分残余**）


| 字段           | 内容                                                                                                                                                                                                                                           |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **位置**       | `src/eywa/kernel/intelligence/plan.py`、`src/eywa/kernel/semantics/memory.py`（`# type: ignore[arg-type]`）；`src/eywa/kernel/semantics/__init__.py`、`src/eywa/open/skills/__init__.py` 等 **宽幅 `__all__`**                                                                                                                                                                                         |
| **描述**       | **`type: ignore`** 已 **大幅收窄**（仓库当前 **1** 处：`listen_ontology`）；包级 **大量再导出** 仍可继续收窄 **`__all__`**，与 **一致**、**简单** 原则有张力。 |
| **对齐原则**     | 宪法 §3.2 **一致**；§3.3 **可读**；§3.8 **简单**                                                                                                                                                                                                             |
| **消除标准（验收）** | **至少削减** 一处 `type: ignore`：以 **`TypedDict`**、**显式转换函数** 或 **narrowing** 替代；**或** 经 **`pyright`/`mypy` 配置** 与 **团队约定** 将忽略 **收束到单行窄规则** 并文档化。**再导出**：**收窄 `__all__`** 或 **文档化「稳定子集」**；**全量测试与类型检查绿**（若 CI 已启用）。                                                                                                         |
| **风险与约束**    | 改变 **公开导入** → **[SDD]** 或 **DeprecationWarning** 周期。                                                                                                                                                                                                        |


---

### BS-ROBUST-003 — 知识抽取与 HTTP 辅助路径的宽捕获 / 静默分支（**已消除 — 归档**）


| 字段           | 内容                                                                                                                                                                                                                                           |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **位置**       | `src/eywa/open/knowledge/pdf_text.py`（`except Exception`）；`src/eywa/foundation/llm/client.py` — `_log_openai_compat_http_error` 内 `response.read()` 的 **`except Exception: pass`**                                                                                                                                                                                         |
| **描述**       | **PDF** 路径 **宽泛捕获** 后转 **`KnowledgeLoadError`**，**无结构化日志**，排障依赖异常串。**HTTP 日志辅助** 中 **`read()` 失败静默吞掉**，呈 **沉默失败** 气味，与 **BS-ROBUST-001** 已加强路径 **不一致**。 |
| **对齐原则**     | 宪法 §3.6 **健壮**（可观测、避免无声失败）                                                                                                                                                                                                             |
| **消除标准（验收）** | **PDF**：失败路径至少 **DEBUG/INFO** 一条（无敏感原文）或 **计数指标**；保持 **`KnowledgeLoadError`** 对外契约。**HTTP**：`read()` 失败 **记录 WARNING**（脱敏）或 **显式注释+计数** 关闭静默；**全量测试绿**；若 **对外异常类型** 变化 → **[SDD]**。                                                                                                         |
| **风险与约束**    | 日志 **不得** 输出 PDF/HTTP **正文明文密钥**；须 **`redact_secrets`** 或截断策略与 **BS-SEC-001** 一致。                                                                                                                                                                                                        |


---

### BS-CLARITY-009 — `Eywa` 母体静态方法面扩张（MVP 5）


| 字段           | 内容                                                                                                                                                                                                                                           |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **位置**       | `src/eywa/kernel/core/eywa.py` — **`Eywa.parse_directive` / `plan` / `decide` / `spirit_for_decision` / `record_evaluation`**；`src/eywa/kernel/core/eywa_mother.py`                                                                                                                                                                                         |
| **描述**       | 母体编排（**FR-U-05～08**）落地后，**用户向 `Eywa` 类** 在 **`kindle`** 之外又承载 **多条静态入口**，与 **`MotherOrchestration`** 并存，呈现 **静态门面膨胀** 与 **规约/设计双锚点**（spec §1.5、design §10.3）成本，与 **BS-CLARITY-004**（`kindle` 电池化）**叠加** 后 **总览难度** 上升。 |
| **对齐原则**     | 宪法 §3.1 **清晰**；§3.8 **简单**                                                                                                                                                                                                             |
| **消除标准（验收）** | **任选**：母体 API **仅** 经 **`MotherOrchestration` 工厂** + `Eywa` 薄别名；或 **`Eywa` 可实例化** 母体上下文（与 SDD §3.13 叙事一致）；**公开签名** 不变或经 **[SDD]**；**全量测试绿**。                                                                                                         |
| **风险与约束**    | 对外 API 变更 → **[SDD]**。                                                                                                                                                                                                        |


---

### BS-REUSE-003 — `bootstrap` 与 `.env.yaml` 合并的多路径重复（MVP 5 / FR-F-07）


| 字段           | 内容                                                                                                                                                                                                                                           |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **位置**       | `src/eywa/kernel/core/bootstrap.py`；`src/eywa/foundation/llm/env_yaml.py` — **`finish_registry_with_env_yaml`**                                                                                                                                                                                         |
| **描述**       | **`bootstrap_from_env` / `bootstrap_from_openai_compat_serial_env` / `bootstrap_from_llm_registry` / `bootstrap_from_injected_llm`** 均在 **registry 填充后** 调用 **`finish_registry_with_env_yaml`**；返回 **六元组** 与 **`KindleBootstrap.eywa_brain_profile_id`** 延伸 **同一套「双源」语义**。若 **design §5.1.2** 调整 **合并顺序**，**多处** 需同步，**DRY** 张力上升。 |
| **对齐原则**     | 宪法 §3.4 **复用**；§3.2 **一致**                                                                                                                                                                                                             |
| **消除标准（验收）** | 提取 **单私有**「**env 注册 + YAML 合并 + brain id 解析 + `_bootstrap_from_filled_registry`**」**或** 等价 **单一装配函数**；**`kindle` / 测例** 可观察行为不变；**全量测试绿**。                                                                                                         |
| **风险与约束**    | 对外行为或 profile 语义变化 → **[SDD]**。                                                                                                                                                                                                        |


---

### BS-CLARITY-010 — `env_yaml._profile_from_yaml_item` 长分支与字段耦合（MVP 5）


| 字段           | 内容                                                                                                                                                                                                                                           |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **位置**       | `src/eywa/foundation/llm/env_yaml.py` — **`_profile_from_yaml_item`** 及 **`merge_env_yaml_profiles_into_registry`**                                                                                                                                                                                         |
| **描述**       | **新建** profile 与 **YAML 覆盖已有** 两分支持 **长条件分支**，字段与 **`LlmProfile`** / **`dataclasses.replace`** 强耦合 **design §5.1.2** 表，**单函数** 修改面大，易漏 **`api_key_env` / `backend_kind`** 边角。 |
| **对齐原则**     | 宪法 §3.1 **清晰**；§3.8 **简单**                                                                                                                                                                                                             |
| **消除标准（验收）** | 拆为 **`_new_profile_from_yaml_item`** + **`_merge_yaml_item_over`** 或 **表驱动** 可覆盖字段列表；**合并顺序** 与 **CHANGELOG / design §5.1.2** 一字不差；**`tests/foundation/test_env_yaml_m5_02.py`** 绿。                                                                                                         |
| **风险与约束**    | 注册语义变更 → **[SDD]**。                                                                                                                                                                                                        |


---

## 3. 修订历史


| 版本    | 日期         | 提交版本                                       | 摘要                                                                                                                               |
| ----- | ---------- | ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------- |
| 0.1.0 | 2026-03-29 | —                                          | 初版：确立为 `docs/bdr/badsmells.md`；元信息表 + 占位节；与宪法 v1.1 对齐。                                                                           |
| 0.1.1 | 2026-03-31 | —                                          | 说明与占位指引 **仅** 指向本文件；不再引用仓库外置草稿路径。                                                                                                |
| 0.2.0 | 2026-03-31 | —                                          | **首次填入具体条目**：BS-CLARITY-001/002、BS-REUSE-001/002、BS-SIMPLE-001（基于 `src/eywa` 代码审阅）。                                              |
| 0.2.1 | 2026-03-31 | `b4431e8e003cd9a3fcc3702e02a91a4030486c56` | 修订历史增列 **提交版本**；识别环节须填 `git rev-parse HEAD`（见 [specification.md §7](./specification.md#7-修订历史表与提交版本门禁)）。                         |
| 0.2.2 | 2026-03-31 | `10886f5f316b91f93a725f794cca2880ac7d1dcd` | 对齐宪法 **§3** 项序：原 **§3.5 简单** 改为 **§3.7**；依据 **v1.3 / v0.2.3**。                                                                   |
| 0.3.0 | 2026-03-31 | `10886f5f316b91f93a725f794cca2880ac7d1dcd` | **重新识别**：保留 BS-CLARITY/REUSE/SIMPLE；新增 **BS-ROBUST-001**（工具/流式错误可观测）、**BS-SEC-001**（脱敏接入与 `base_url` 治理），对齐宪法 **§3.5～§3.6**。     |
| 0.3.1 | 2026-03-31 | `2b18d6e8603d3cf51bbbe92aec2db4e422ca9706` | 对齐宪法 **v1.4**：**§3** 增 **一致** 后各条 **对齐原则** 与 **§3.6～§3.7**（健壮～安全）引用顺延编号。                                                         |
| 0.4.0 | 2026-03-31 | `2b18d6e8603d3cf51bbbe92aec2db4e422ca9706` | **重新识别**：增 **§2.0** 索引（**B-T01～B-T07** 与条目状态）；刷新 **BS-CLARITY-001** 残余描述；新增 **BS-ROBUST-002**、**BS-SEC-002**、**BS-CLARITY-003**。 |
| 0.4.1 | 2026-03-31 | `413820745e1829aa462a947562a8f1192def0132` | **B-T08** 完成：**§2.0** 将 **BS-ROBUST-002** 标为已消除。                                                                                 |
| 0.4.2 | 2026-03-31 | `d5c858d1b3850c8b6a4930f3d2a6545188af126b` | **B-T09** 完成：**§2.0** 将 **BS-SEC-002** 标为已消除；`**OpenAICompatibleClient`** 构造期 `**validate_llm_base_url**`。                       |
| 0.5.0 | 2026-04-10 | — | **BDR 再识别**：修复 **BS-REUSE-002** 消除标准 **截断**；刷新 **BS-CLARITY-001/003** 行数、**BS-SIMPLE-001** 归档与 **BS-REUSE-002** 归档表述；新增 **BS-SIMPLE-002**、**BS-CLARITY-004～008**、**BS-CONSIST-001**、**BS-ROBUST-003**；**§2.0** 索引同步；依据 **specification v0.2.6**。 |
| 0.6.0 | 2026-04-15 | `b5302052c5793c26b507bbc090971a356953b45e` | **代码审阅 + MVP 5 后**：**§2.0** 与 **`tasks.md` B-T10～B-T18** 对齐（**BS-CLARITY-003**、**004**、**005～008**、**BS-ROBUST-003** 标 **已消除**；**BS-SIMPLE-002**、**BS-CONSIST-001** **部分残余**）；刷新 **Spirit** 行数、**`type: ignore`** 计数；新增 **BS-CLARITY-009**、**BS-REUSE-003**、**BS-CLARITY-010**（MVP 5）；依据 **specification v0.2.7**。 |
| 0.7.0 | 2026-04-15 | `ac7e3487124b6ac048cbf40c720a23ae5e079263` | **B-T19～B-T21 完成**：**BS-CLARITY-009**、**BS-REUSE-003**、**BS-CLARITY-010** 由开放改为已消除；索引与 `tasks.md` 状态同步。 |


