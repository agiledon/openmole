# 坏味道驱动重构宪法（BDR Constitution）

**版本**：1.5  
**状态**：**生效**（2026-03-29；**v1.1** 确立 **`badsmells.md`** 为坏味道环节唯一规约载体、任务与分析闭环；**v1.2** 确立 **`badsmells.md` / `tasks.md` / `analysis.md` 修订历史「提交版本」列** 及 [specification.md](./specification.md) **§7** 门禁；**v1.3** 扩充 **§3** 增 **健壮**、**安全**；**v1.4** 在 **§3** **可读** 前增 **一致**；**v1.5** 仅 **依据链** 对齐 **`badsmells` v0.6.0** 与 BDR 下游版本，**§3～§7 正文条款无变更**）  
**适用范围**：本仓库内一切 **BDR 流程** 下的重构活动；**若重构改变对外可观察行为**，须同步走 **SDD** 修订规约/设计并验收。

**地位**：本文档为 **BDR 的最高指导规范**；[specification.md](./specification.md)、[badsmells.md](./badsmells.md)、[tasks.md](./tasks.md)、[analysis.md](./analysis.md) 均 **不得** 与本文冲突；冲突时 **修订下游文档**（或经维护者批准修订宪法）。

**依据**：[BDR README](./README.md)；产品级工程宪法见 [constitution.md](../sdd/constitution.md)。

---

## 1. 使命

在 **坏味道规约**（**[badsmells.md](./badsmells.md)**）与 **任务分解**（**[tasks.md](./tasks.md)**）的约束下，以 **小步、可测试、可回滚** 的方式改进代码内部质量；**消除坏味道为驱动力**，消除计划范围内的坏味道后，本轮 BDR **宣告结束**。

---

## 2. BDR 权威工件与坏味道规约文档

### 2.1 坏味道（badsmells）环节的产出

- **坏味道环节** 所生成的 **坏味道规约文档**（承载具体条目、统计与元数据）**统一命名为 `badsmells.md`**。  
- **存放位置**：**仅** **`docs/bdr/badsmells.md`**（与本目录其他 BDR 文档同级），**不得** 使用其它文件名作为该环节的正式规约载体。  
- **版式**：与 **constitution / specification / tasks / analysis** 保持一致 —— 文首 **元信息表**（**版本、状态、日期、依据** 等）+ 结构化正文；条目的 **字段、编号、验收口径** 遵循 [specification.md](./specification.md)。  
- **修订历史**：**`badsmells.md`**、**`tasks.md`**、**`analysis.md`** 文末修订历史表 **必须** 含 **提交版本** 列；**识别坏味道 / 分解任务 / 分析** 三环节在升版或新增修订行时 **须填写** `git rev-parse HEAD`，规则见 [specification.md §7](./specification.md#7-修订历史表与提交版本门禁)。

### 2.2 元规约与具体规约的分工

- **[specification.md](./specification.md)**：**元规约**，规定「坏味道条目应如何写、如何编号、如何与 SDD 联动」等 **格式与门禁**；**不替代** `badsmells.md` 中的 **具体清单**。  
- **[badsmells.md](./badsmells.md)**：**具体坏味道规约文档**，在宪法与元规约约束下 **持续迭代** 的 **事实源（source of truth）**。

### 2.3 任务（tasks）环节

- **[tasks.md](./tasks.md)** 中的重构任务 **必须** 根据 **`badsmells.md` 的当前内容** 进行 **规划与拆分**（每条任务须能追溯到 `badsmells.md` 中的条目或 ID）。  
- **禁止** 在脱离 `badsmells.md` 的情况下凭空编造与 BDR 无关的大规模重构任务清单（若发现新坏味道，**先更新 `badsmells.md`**，再走 **分析** 更新任务）。

### 2.4 分析（analyze）环节与文档迭代

- **`badsmells.md` 允许不断迭代**（增删改条目、调整版本号与修订说明）。  
- 每当 **`badsmells.md` 的版本或实质性内容发生变化** 时，**必须** 进入 **分析环节**（**[analysis.md](./analysis.md)**）：  
  1. 比对 **坏味道条目集合** 与 **`tasks.md` 中的任务**（覆盖、依赖、DoD 是否仍有效）；  
  2. 发现 **遗漏任务、过时任务、与条目不一致** 等情况时，**先更新 `tasks.md`**（及必要时 **specification.md** / **analysis.md** 本体的结论段），**再** 按更新后的任务执行重构。  
- **冲突处理**：与 SDD 一致 —— **先改文档、后改代码**；若分析发现宪法与 `badsmells.md` 不可兼得，**先动 `badsmells.md` / specification 或启动宪法修订流程**，不得静默硬改代码绕过文档。

---

## 3. 重构第一性原则

重构现有代码时，需要在 **不改变既有代码外部功能**（对调用方与测试所固定的可观察行为）的基础上，提升代码的 **内部质量**，包括：

1. **清晰**：保持代码结构的清晰度，确保各个模块的职责分配合理，边界清晰，满足 **高内聚、低耦合** 原则。  
2. **一致**：确保设计与编码实现的一致性；确保编码风格的一致性；确保错误处理的一致性；确保接口定义和通信方案的一致性；确保对外访问的一致性。  
3. **可读**：类、方法、变量命名遵循编码规范，体现业务或领域含义；在确保命名可理解基础上，名称尽可能简短；若实现逻辑相对复杂，可添加 **适度** 注释。  
4. **复用**：遵循 **DRY**，避免不必要的重复；清晰定义领域/模型对象，并遵循 **信息专家模式** 与 **迪米特法则**；职责分配遵循 **单一职责** 与 **接口隔离**；合理运用函数与 lambda 做 **小粒度** 抽象。  
5. **扩展**：**不对未来需求做无依据假设**；遵循 **开放封闭** 与 **依赖倒置**；接口定义应 **规范、可版本化**，在可行范围内考虑 **向前/向后兼容**；抽象应能以 **最小修改成本** 接入更多基础设施、第三方框架与外部系统；具备 **可测试性**，主体逻辑可通过 mock **易于** 编写单元测试。  
6. **健壮**：确保代码能够 **正确处理** 可预见的 **错误与异常情形**；采用 **分级** 方式区分可恢复错误、须向上传递的故障与不应静默掩盖的严重问题；对异常路径以 **适当级别** 记录日志（含必要上下文），**避免** 因未处理异常、不当吞异常或过度宽泛捕获导致 **未定义行为** 或非预期的 **进程崩溃**。与「不改变外部可观察行为」并存时，以 **测试与规约** 固定语义。  
7. **安全**：系统性 **降低** 常见编码与运行环境中的 **安全风险**，包括但不限于：**输入与输出**（校验、编码、路径规范化，防范注入、路径穿越与 SSRF 等）、**机密与隐私**（密钥与令牌不入版本库与明文日志，敏感数据脱敏与最小留存）、**最小权限与暴露面**（默认安全、关闭不必要能力与调试接口）、**依赖与供应链**（可信来源、关注已知漏洞通告）。触及网络、文件系统、子进程、反序列化等边界时，**优先** 消除已知反模式。  
8. **简单**：以上第 **1～7** 项按 **重要性先后** 排列；在满足上述要求的基础上，遵循 **奥卡姆剃刀**——若无必要，勿增实体（模块、类、方法、函数）。

**与 SDD 宪法 §7 的关系**：上列八条是对 **重构场景** 的展开；与 SDD **§7.3 重构第一性原则** 并行适用，**冲突时以更保守、更利于可验证性的条款为准**，并在 `analysis.md` 中记录取舍。

---

## 4. 重构的标准步骤

**步骤 1：识别坏味道**  
依据 **[badsmells.md](./badsmells.md)** 与 [specification.md](./specification.md)，明确待消除条目与优先级。

**步骤 2：确定测试覆盖**  
确定存在坏味道的代码 **是否已被单元测试（或等价自动化测试）覆盖**；若 **没有**，**先为其编写测试**，锁定当前行为。

**步骤 3：运行测试并修复至绿**  
运行单元测试，确保 **全部通过**；若失败，**首先修复失败的测试**（或修正测试与规约的一致性），直至通过。

**步骤 4：重构以消除坏味道**  
在 **测试绿** 的前提下实施最小必要重构，对准已识别的坏味道，避免无关扩散。

**步骤 5：回归与确认**  
再次运行测试，确保重构 **未破坏** 既有功能；测试通过后，**经用户（维护者）确认**，再对 **下一坏味道** 重复 **步骤 1～5**（或按 [tasks.md](./tasks.md) 进入下一任务）。

---

## 5. 重构过程的基本原则

1. **必须** 按 **§4 重构的标准步骤** 推进，**不得** 跳过「补测—测绿—重构—再测绿」的闭环。  
2. **每一步** 重构结束时，代码须保持 **可导入、可运行、测试可执行**（不可用状态不得作为「中间态」合入主线，除非流程明确允许使用短生命周期分支且仍须本地测绿）。  
3. **坏味道是驱动力**：任务与提交应对齐 **`badsmells.md`** 中的条目；**计划范围内坏味道均已消除或显式降级（文档记载原因）** 后，本轮 BDR 结束。

---

## 6. BDR 流程门禁

| 阶段 | 门禁 |
|------|------|
| **`badsmells.md`** | 与本文 **§3～§5** 无冲突；条目可验证、可映射到任务；版式符合 **§2.1**；修订历史含 **提交版本**（见 [specification.md §7](./specification.md#7-修订历史表与提交版本门禁)） |
| **任务** | 由 **`badsmells.md`** 拆分而来；有依赖顺序；每条具备完成定义（DoD）；**`tasks.md`** 修订历史含 **提交版本** |
| **分析** | **`badsmells.md` 变更后** 必须跑差分并同步 **tasks.md**；发现宪法 / 元规约 / 具体规约 / 任务不一致时，**先改文档**；**`analysis.md`** 修订历史含 **提交版本** |
| **实现** | **每任务** 结束后 **用户确认**；对外行为变更走 SDD |

---

## 7. 修订与版本

| 版本 | 日期 | 提交版本 | 摘要 |
|------|------|----------|------|
| 1.0 | 2026-03-29 | — | 初版：重构第一性原则、标准步骤、过程基本原则、门禁摘要。 |
| 1.1 | 2026-03-29 | — | **最高规范** 表述；**`docs/bdr/badsmells.md`** 为坏味道环节唯一命名与位置；任务须据其拆分；**`badsmells.md` 迭代 → 分析 → 更新 `tasks.md`**；章节重编号（原 §2～§6 → §3～§7）。 |
| 1.2 | 2026-03-31 | `b4431e8e003cd9a3fcc3702e02a91a4030486c56` | **§2.1** 与 **§6** 对齐 [specification.md §7](./specification.md#7-修订历史表与提交版本门禁)：**识别 / 任务 / 分析** 三文档修订历史 **提交版本** 列；**§7** 本表同步增列。 |
| 1.3 | 2026-03-31 | `10886f5f316b91f93a725f794cca2880ac7d1dcd` | **§3** 在 **简单** 前增 **健壮**（分级异常、日志、避免非预期崩溃）、**安全**（输入/机密/权限/依赖等常见风险原则）；**简单** 顺延为第 7 项；与 SDD 关系改为「上列七条」。 |
| 1.4 | 2026-03-31 | `2b18d6e8603d3cf51bbbe92aec2db4e422ca9706` | **§3** 在 **可读** 前增 **一致**（设计/实现、风格、错误处理、接口与通信、对外访问）；后续条目顺延编号；**简单** 改为第 8 项，其「以上第 1～7 项」表述同步；与 SDD 关系改为「上列八条」。 |
| 1.5 | 2026-04-15 | `b5302052c5793c26b507bbc090971a356953b45e` | **依据** 三角对齐 **`badsmells.md` v0.6.0**、[specification.md](./specification.md) **v0.2.7**、[tasks.md](./tasks.md) **v0.7.0**、[analysis.md](./analysis.md) **v0.4.0**；**§3～§7 正文条款无变更**。 |

---

**确认项（维护者）**

- [ ] 同意 BDR 与 SDD 并列，对外行为变更须回 SDD。  
- [ ] 同意 **§3～§5** 作为本仓库 BDR 活动的强制约束。  
- [ ] 同意 **`badsmells.md`** 置于 **`docs/bdr/`** 且为坏味道规约 **唯一正式文件名**。  
- [ ] 同意每任务结束需 **用户确认** 后再继续。  
- [ ] 同意 **`badsmells.md` 变更后** 必须经 **分析** 更新 **tasks.md**。  
- [ ] 同意 **`badsmells.md` / `tasks.md` / `analysis.md`** 修订历史含 **提交版本** 列，并按 [specification.md §7](./specification.md#7-修订历史表与提交版本门禁) 填写。
